#include <stdbool.h>

/*
    check_coders_burnout(t_coder *coders)
    elapsed = get_time_in_ms() - coder->last_compile_start
    nb_comp_req = coder->data->number_of_compile_required
    cmp_count = coder->compilation_count
    if (
        cmp_count != nb_comp_req &&
        elapsed >= coder->data->time_to_burnout
    )
        return (true);
    return (false);
*/

/*
    is_simulation_finished()
*/